node(){
    stage('Checkout Test Framework'){
        deleteDir()
        checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'bot_policycenter', url: 'https://10.150.234.25/Guidewire/policycenter-automation.git']]])
    }
    stage("Executing Test"){
        sh "mvn clean install"
    }
    stage("Publish Test Results"){

    }
}